name: Run isort
on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/chekout@v3
      - uses: isort/isort-action@v1.0.0
        with:
          reqirements-files: "pyproject.toml"